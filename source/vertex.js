class Vertex {
     constructor(vertexType, name, x, y) {
          this.vertexType = vertexType;
          this.name = name;
          this.x = x;
          this.y = y;
     }
     setColor(color) {
          this.color = color;
     }
     draw() {

     }
}
